// metodos de cadenas 

let cadena = 'abc ';
let cadena2 = 'cadena';

// # .concat('string'), junta dos o mas cadenas y retorna una nueva

// # .startsWith('string'), si una cadena empieza con los caracteres de otra cadena devuelve true, de lo contrario devuelve false

// # .endsWith('string'), si una cadena termina con los caracteres de otra cadena devuelve true, de lo contrario devuelve false

// # includes('string'), si una cadena se puede encontrar dentro de otra cadena, devuelve true, de lo contrario false

// # indexOf('string'), devuelve el indice del primer caracter de la cadena, si no encuentra nada, devuelve -1

// # lastIndexOf('string'), devuelve el ultimo indice del primer caracter de la cadena, si no encuentra nada, devuelve -1

// # padStart(numero de caracteres,'string'), rellena al principio de la cadena con los caracteres deseados 

// # padEnd(numero de caracteres,'string'), rellena al final de la cadena hasta el numero de caracteres especificados con los caracteres deseados 

// # repeat(numero de repeticiones), repite la cadena el numero de repeticiones especificado, rango=(positivos)

resultado = cadena.repeat(3); 

document.write(resultado)